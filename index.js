var dateToRemember = '2022-4-15';

function timeUntil(endTime) {
    const totalTime = Date.parse(endTime) - Date.parse(new Date()); //use the current date minus the anticipated date in preparation for counting the time between
}